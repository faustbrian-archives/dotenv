{
	"name": "@kodekeep/dotenv",
	"version": "0.1.0",
	"description": "DX Focused API for working with .env files",
	"license": "MIT",
	"repository": "https://github.com/kodekeep/dotenv",
	"author": "KodeKeep <hello@kodekeep.com>",
	"main": "dist/index",
	"scripts": {
		"build": "tease build",
		"format": "tease format",
		"lint": "tease lint",
		"prepare": "tease install && tease build",
		"publish": "npm run prepare && npm publish --access=public",
		"specs": "tease specs",
		"test": "tease test"
	},
	"files": [
		"dist"
	],
	"dependencies": {
		"@hapi/joi": "^17.1.1"
	},
	"devDependencies": {
		"@konceiver/tease": "^2.0.0"
	},
	"types": "dist/index"
}
